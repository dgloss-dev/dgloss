'use client';
import { Logo } from '@workspace/ui/components/atoms/logo';

const Page = () => {
  return (
    <section className=" w-full h-full bg-primary ">
      <Logo />
    </section>
  );
};

export default Page;
